# scripts/.dev.env.example — template for remote-control debugging config.
# Copy to scripts/.dev.env (gitignored) and fill with the live values.
# Sourced automatically by scripts/dev.sh.

REMOTE_HOST=127.0.0.1:8788
REMOTE_TOKEN=
REMOTE_TIMEOUT=5
